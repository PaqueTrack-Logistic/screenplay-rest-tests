serenity {
    project.name = "PaqueTrack Screenplay REST - Fabrica Escuela 2026-1"
    take.screenshots = DISABLED
    test.root = co.edu.udea.calidad.paquetrack
    report {
        encoding = "UTF-8"
    }
    requirement {
        types = "feature"
    }
}

# La URL base del API NO se quema en el codigo ni se duplica aqui: la gobierna
# RestApiConfig con prioridad -Drestapi.baseurl > .env (RESTAPI_BASEURL) >
# restapi.properties > default. Asi hay una unica fuente de verdad.
